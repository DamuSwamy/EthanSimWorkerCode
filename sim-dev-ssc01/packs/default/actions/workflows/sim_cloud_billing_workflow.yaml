version: 1.0
tasks:
  # [170, 189]
  task1:
    action: sim_cloud_billing.get_iaas_vcloud_data
    next:
      - do:
          - insert_into_iaas_cloud_temp
        when: <% succeeded() %>
  # [170, 74]
  remove_data_iaas_vcloud_temp:
    action: sql.delete
    next:
      - do:
          - task1
        when: <% succeeded() %>
    input:
      log_level: DEBUG
      table: Iaas_vCloud_Temp
      connection: cloudmanagement
  # [166, 303]
  insert_into_iaas_cloud_temp:
    action: sql.insert_bulk
    input:
      log_level: DEBUG
      table: Iaas_vCloud_Temp
      connection: cloudmanagement
