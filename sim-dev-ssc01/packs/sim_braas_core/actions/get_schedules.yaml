---
name: get_schedules
runner_type: python-script
description: Retrieves the schedules from the Braas server. 
enabled: true
entry_point: get_schedules.py
parameters:
  braas:
    type: "string"
    description: Pre-Configured braas connection details
    required: true
  access_token:
    type: "string"
    description: Access token if you have the token already else blank
    required: false

