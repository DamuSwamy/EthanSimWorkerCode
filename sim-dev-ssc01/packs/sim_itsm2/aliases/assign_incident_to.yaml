---
name: "sim_itsm2.assign_incident_to"
action_ref: "sim_itsm2.assign_incident_to"
description: "Assign an incident to a username"
formats:
    - "assign incident {{number}} to {{name}}"
ack:
    format: "Reassiging incident"
result:
    format: "incident reassigned"
