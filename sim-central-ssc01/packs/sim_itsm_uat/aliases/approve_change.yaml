---
name: "sim_itsm_uat.approve_change"
action_ref: "sim_itsm_uat.approve_change"
description: "Set the approval status of a Change Request to approved."
formats:
    - "approve {{number}}"
ack:
    format: "Approving change"
result:
    format: "Change approved"
