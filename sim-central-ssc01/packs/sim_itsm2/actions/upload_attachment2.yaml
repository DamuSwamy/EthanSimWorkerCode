---
name: "upload_attachment2"
runner_type: "python-script"
description: "Uploads an attachment to a record in a ServiceNow Table"
enabled: true
entry_point: "upload_attachment2.py"
parameters:
  table:
    type: "string"
    description: "Table to take action on"
    required: true
  number:
    type: "string"
    description: "number of the record"
    required: true
  file:
    type: "string"
    description: "Path to local file to attach"
    required: true
